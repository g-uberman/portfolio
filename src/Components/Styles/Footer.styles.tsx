import styled from "styled-components";

export const StyledFooter = styled.footer`
    text-align: center;
    font-size: 0.8em;
    background: #fff;
    padding: 10px;

    a, a:visited {
    color: #000;
    }
`