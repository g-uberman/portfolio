import styled from "styled-components";

export const StyledFooter = styled.footer`
    text-align: center;
    font-size: 0.8em;
    margin-bottom: 0.3em;

    a, a:visited {
    color: #000;
    }
`