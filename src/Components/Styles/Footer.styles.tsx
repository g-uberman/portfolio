import styled from "styled-components";

export const StyledFooter = styled.footer`
    text-align: center;
    font-size: 0.8em;
    background: #fffffff5;
    padding: 10px;

    a, a:visited {
    color: #000;
    }
`